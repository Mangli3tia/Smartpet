# utils/feeder.py
def feed():
    """执行喂食动作，返回 True 表示成功"""
    print("🍖 执行喂食动作（模拟）")
    # 真实硬件代码示例（Raspberry Pi GPIO）：
    # import RPi.GPIO as GPIO
    # FEEDER_PIN = 18
    # GPIO.setmode(GPIO.BCM)
    # GPIO.setup(FEEDER_PIN, GPIO.OUT)
    # GPIO.output(FEEDER_PIN, True)
    # time.sleep(1)
    # GPIO.output(FEEDER_PIN, False)
    return True